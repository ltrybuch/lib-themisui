import * as angular from "angular";
import "angular-mocks";

describe("ThemisComponents: Component: Template", () => {
  beforeEach(angular.mock.module("ThemisComponents"));

  it("should have tests, get to it!");
});
